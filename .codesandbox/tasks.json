{
  "version": "0.0.1",
  "tasks": {
    "start-backend": {
      "name": "Backend (FastAPI)",
      "command": "find / -name 'requirements.txt' -path '*/crypto-interview/*' 2>/dev/null | head -5 && find / -name 'main.py' -path '*/crypto-interview/*' 2>/dev/null | head -5 && echo 'PWD:' && pwd && ls",
      "runAtStart": true
    }
  }
}
