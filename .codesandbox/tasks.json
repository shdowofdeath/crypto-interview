{
  "version": "0.0.1",
  "tasks": {
    "start-backend": {
      "name": "Backend (FastAPI)",
      "command": "cd backend && pip install -r requirements.txt && uvicorn main:app --host 0.0.0.0 --port 8000 --reload",
      "runAtStart": true,
      "preview": {
        "port": 8000
      }
    },
    "start-frontend": {
      "name": "Frontend (Vite)",
      "command": "cd frontend && npm install && npm run dev",
      "runAtStart": true,
      "preview": {
        "port": 5173
      }
    }
  }
}
